/**Jenkinsfile for creating triggers for PollSCM, WebHook, Cron
**/


pipeline{
    agent any
    triggers{
        pollCM('H/15 * * *')//pollSCM trigger for 15 min everyday
        cron('0 2 * * *')//Cron trigger for 2AM everyday
    }
    stages{
        stage('stage1'){
            steps{
                echo "stage1 complete" //workflow commands
            }
        }
    }
}